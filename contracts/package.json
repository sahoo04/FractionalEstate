{
  "name": "fractionalstay-contracts",
  "version": "1.0.0",
  "description": "FractionalStay smart contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "deploy:property-only": "hardhat run scripts/deploy-property-only.ts --network arbitrumSepolia",
    "deploy:marketplace-only": "hardhat run scripts/deploy-marketplace-only.ts --network arbitrumSepolia",
    "cancel:old-listings": "hardhat run scripts/cancel-old-listings.ts --network arbitrumSepolia",
    "check:old-listings": "hardhat run scripts/check-old-listings.ts --network arbitrumSepolia",
    "mint-usdc": "hardhat run scripts/mint-usdc.ts --network arbitrumSepolia",
    "setup": "hardhat run scripts/setup.ts --network arbitrumSepolia"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

